#include <stdio.h>

/**
 * main - Entry point
 * print - prints exactly with proper grammar, but the outcome is a piece of art,
 * Return: Always 0 (Success)
 */
int main(0)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
